import { Showcase } from "@/components/showcase";

// The original 4-room, shop-the-look demo (data/property.json). Preserved here after
// the main page moved to the full curated property.
export default function Page() {
  return <Showcase />;
}
