# web/ — property page (deferred)

The `latentframe.ai/[property]` page: the reveal reel, before/after sliders, and — the
actual business — **lead capture**, with affiliation later. Next.js + pnpm workspace.

Deliberately not scaffolded yet. It gets built at the **ASSEMBLE** stage, once the
one-room experiment proves the video wow lands. Building the trap before we have bait is
backwards.
