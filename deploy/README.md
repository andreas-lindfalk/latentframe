# deploy/ — Docker + Cloud Run (placeholder)

Target: containerized Go services on GCP Cloud Run, with the ingest worker consuming a
job queue (video restage is a minutes-long async job). Deployment scaffolding to crib
from the author's `videra` project (Dockerfile + runtime profiles + Cloud Run parity).

Added when there's a service to deploy — after the local pipeline works end to end.
